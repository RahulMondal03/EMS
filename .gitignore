# Python bytecode cache
__pycache__/
*.py[cod]

# Node / Playwright (e2e suite only — the app itself has no dependencies)
node_modules/
test-results/
playwright-report/
blob-report/
playwright/.cache/
